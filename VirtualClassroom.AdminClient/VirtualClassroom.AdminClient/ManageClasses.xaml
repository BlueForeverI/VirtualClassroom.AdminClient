<Page x:Class="VirtualClassroom.AdminClient.ManageClasses"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Manage Classes">
    <Grid>
        <GroupBox Header="Classes">
            <WrapPanel>
                <DataGrid x:Name="dataGridClasses" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Number" Binding="{Binding Path=Number}" />
                        <DataGridTextColumn Header="Letter" Binding="{Binding Path=Letter}" />
                    </DataGrid.Columns>
                    <StackPanel>
                        <Button x:Name="btnAddClass" Click="btnAddClass_Click">Add Class</Button>
                        <Button x:Name="btnRemoveClass" Click="btnRemoveClass_Click">Remove Class</Button>
                    </StackPanel>
            </DataGrid>
            </WrapPanel>
        </GroupBox>
    </Grid>
</Page>
